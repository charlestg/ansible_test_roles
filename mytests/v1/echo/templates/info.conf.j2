# Sample template
# https://gist.github.com/carlessanagustin/f935fac7b61661b31d74

#   GROUPS tests
{{ groups["tests"][0] }}

#   group_vars
test_var: {{ test_var}}

#   all vars
all_vars: {{ all_vars }}




#
#   Default variables
#
ansible_ssh_user            {{ ansible_ssh_user }}
inventory_dir               {{ inventory_dir }}
inventory_file              {{ inventory_file }}
inventory_hostname_short    {{ inventory_hostname_short }}
inventory_hostname          {{ inventory_hostname }}
hostvars                    {{ hostvars }}
group_names                 {{ group_names }}
groups                      {{ groups }}
defaults                    {{ defaults }}
environment                 {{ environment }}
play_hosts                  {{ play_hosts }}

#
#HOSTVARS (ANSIBLE GATHERED, group_vars, host_vars) :
#
{{ hostvars[inventory_hostname] | to_yaml }}


#
# PLAYBOOK VARS:
#
{{ vars | to_yaml }}

#
#   Facts
#
ansible_distribution                # needs discovery
ansible_distribution_release        # needs discovery
ansible_distribution_version        # needs discovery
ansible_fqdn                        # needs discovery
ansible_hostname                    # needs discovery
ansible_os_family                   # needs discovery
ansible_pkg_mgr                     # needs discovery
ansible_default_ipv4.address        # needs discovery
ansible_default_ipv6.address        # needs discovery

